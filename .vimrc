// 打开行号
# set number

# 
# set nocompatible

# 打开语法高亮
# syntax on

# 在底部显示当前处于命令模式还是插入模式
# set showmode

# 在底部显示当前处于命令模式下当前键入的指令
# set shwocmd

# 开启文件类型检查，并载入对应的文件缩进规则
# filetype indent on

# 按下回车键，下一行的缩进会自动跟上一行的缩进保持一致 
# set autoindent

# 光标所在行高亮 
# set cursorline